version: '3'

services:
  db:
    image: nouchka/sqlite3:latest
    volumes:
      - .:/db
    environment:
      - SQLITE3=db.sqlite3
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
